<!-- <ul class="clearing-thumbs" data-clearing>
			<% @headlines.map do |headline| %>
				
				<% if headline.image_url.map.any? %>
				<li><a class="ttt th th-item"><%= image_tag headline.image_url.first, data: { caption: "<a href=#{headline_path(headline)}>#{headline.description}</a>" } %></a></li>

				<% else %>
					<li><a class="ttt th th-item"><%= image_tag('espn_logo_frozen.png', data: { caption: "<a href=#{headline_path(headline)}>#{headline.description}</a>" }) %></a></li>
				<% end %>
				<% end %>
			</ul> -->